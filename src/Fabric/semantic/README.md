# Semantic

This layer holds semantic definitions that sit on top of curated gold data.

## Suggested use
- Build Power BI semantic models on top of `gold` outputs.
- Keep business measures and naming conventions aligned with dbt model definitions.
